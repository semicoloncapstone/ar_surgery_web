{{#if isUserFormEditing }}
    <div class="ui modal auth">
      <div class="header">Create New Class</div>
      <div class="content">
        <div class="ui form">
          <div class="inline field">
            <label>Class Name</label>
            {{input value=className type="text" placeholder="Enter Class Name" }}
          </div>
          <div class="inline field">
            <label>Class Size</label>
            {{input value=classSize type="text" placeholder="Enter Class Size"}}
          </div>
          <div class="inline field">
            <label>Program</label>
            {{input value=program type="text" placeholder="Enter Program"}}
          </div>
          <div class="inline field">
            <label>School</label>
            {{input value=school type="text" placeholder="Enter School"}}
          </div>
        </div>
      </div>
      <div class="actions">
        <div class="w3-button w3-green w3-round-large" {{action "save"}}>
          Save Class
        </div>
        <div class="w3-button w3-red w3-round-large" {{action "cancel"}}>
          Cancel
        </div>
      </div>
    </div>
{{/if}}

<button class="w3-button w3-green w3-round-large w3-right" {{action 'addNewClass'}}>
  <i class="fa fa-plus"></i>
  Add New Class
</button>
<br><br>

